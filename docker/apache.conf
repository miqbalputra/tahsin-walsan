ServerName localhost

<Directory /var/www/html>
    AllowOverride All
    Options -Indexes
    Require all granted
</Directory>

<FilesMatch "^\.">
    Require all denied
</FilesMatch>

<FilesMatch "\.(sql|zip|xlsx|md)$">
    Require all denied
</FilesMatch>
